# 合并版 ZSK 使用说明（Codex / WorkBuddy）

## 先记住一件事

客户只需要说“使用 zsk-router”。不要单独调用 zsk-ruku、zsk-zhishi、zsk-duibiao、zsk-profile 或 markitdown-skill；它们都是 Router 后台能力。

## 到底有几次确认

授权和内容确认是两类不同的事：

| 场景 | 可能出现的授权 | 建库确认 | 内容确认 1 | 内容确认 2 |
|---|---:|---:|---:|---:|
| 第一次新建飞书/Obsidian 知识库 | 飞书未授权时 1 次 | 1 次，确认库名和位置 | 每份资料 1 次，确认 03/04/05 | 每份资料 1 次，确认完整草稿 |
| 绑定已有知识库 | 飞书未授权时 1 次 | 没有，但会只读检查九根结构 | 每份资料 1 次 | 每份资料 1 次 |
| 已绑定后继续上传资料 | 授权失效时才出现 | 没有 | 每份资料 1 次 | 每份资料 1 次 |

所以，一份资料正式入库始终只有两次内容确认：

1. 分类确认：确认进入 03 业务知识、04 内容方法或 05 主体画像。
2. 草稿确认：完整看完即将发布的知识页，再确认发布。

首次授权、建库位置确认都不冒充这两次内容确认。草稿、分类或 PDF/PPT 页面图片发生变化，原确认自动失效，必须重新确认。

## 场景一：第一次新建知识库

对 Codex 或 WorkBuddy 说：

> 请使用 zsk-router，在飞书为我创建一个私有知识库，名称为“我的豪宅知识库”。先检查，不要直接创建。

Router 会按下面的顺序处理：

1. 检查本机组件、飞书用户授权和建库权限。
2. 如果未授权，Agent 主动打开飞书原始授权页，并在对话中展示链接和二维码。客户点击或扫码后只需回复“已授权”。
3. 展示库名和目标位置，等待建库确认。
4. 创建 01—07、AGENTS、README 九个根对象并回读。
5. 只有九个对象全部存在且规则可读，才返回“知识库已创建”和飞书链接。

Obsidian 流程相同，但会先展示完整本地路径。不会默认写桌面、不会覆盖已有目录，也不会自动使用个人 Vault。

## 场景二：使用已有知识库

对 Codex 或 WorkBuddy 说：

> 请使用 zsk-router，把这个飞书 Wiki 作为我的已有主知识库：<飞书链接>。只检查并绑定，不要新建另一个库。

Router 会只读检查：账号授权、链接归属、九个根对象、AGENTS 和 README。全部通过后才保存为唯一当前主绑定。已有库结构不完整时不会擅自补建，也不会把它错误标记为可用；应先明确选择“修复这个库”还是“另建一个库”。

## 场景三：上传 PDF 或 PPT

在当前 Codex / WorkBuddy 任务中拖入文件，然后说：

> 请使用 zsk-router 处理我刚拖入的 PDF（或 PPT），按豪宅知识库规则入库。

处理顺序如下：

1. 只读取本次拖入的文件，不扫描其他目录。
2. PDF/PPTX 同时生成可读 Markdown、逐页 PNG 和逐页 OCR；任何页面缺失、顺序错误或 OCR 失败都停止到 02。
3. 给出一次 03/04/05 分类建议和依据，等待第一次内容确认。
4. 按豪宅策略生成正式草稿：03 逐字事实、04 七段拆解、05 三层画像；每条内容引用真实页码。
5. 完整展示草稿，等待第二次内容确认。
6. 飞书发布时，把页面图片插入 01 来源文档，也把实际引用页插入正式知识页。
7. 回读正文、图片标记和稳定对象引用；成功后返回来源链接、正式知识页链接和本地发布收据位置。

## Codex 与 WorkBuddy 的差别

- Codex：默认安装目录通常是 `~/.codex/skills`，仓库安装器可自动选择；安装后重新打开一个任务，让宿主重新发现 Skill。
- WorkBuddy：不同版本的 Skills 目录可能不同。先让 WorkBuddy 告诉你“当前实际 Skills 目录”，再将该绝对路径传给 `install.py --dest`；不要猜目录，也不要同时安装到多个位置。
- 两者使用同一个公开入口、同一个确认合同和同一套 `.zsk-runtime` 状态。运行状态不保存飞书 token、设备挑战码或客户正文。

Windows 示例：

```powershell
py .\install.py --dest "C:\实际\Skills目录" --install-markitdown
py .\install.py --dest "C:\实际\Skills目录" --doctor
```

macOS / Linux 示例：

```bash
python3 ./install.py --dest "/实际/Skills目录" --install-markitdown
python3 ./install.py --dest "/实际/Skills目录" --doctor
```

安装器发现同名目录会停止，不覆盖。需要升级时，先由 Agent 对比旧目录与这个仓库，再做明确的备份和替换操作。

## 你会看到的状态

- `confirmation_required`：等待建库确认，不代表已经创建。
- `awaiting_classification`：等待第一次内容确认。
- `awaiting_draft_confirmation`：等待第二次内容确认。
- `pending`：飞书发布中断但有可继续的阶段收据，不等于发布成功。
- `blocked`：权限、隐私、结构、来源或回读失败，不能继续写入。
- `published`：来源文档和正式页都已写入，正文及图片已经回读，双链接可返回。

## 当前边界

- 支持 MD、TXT、CSV、JSON、HTML、DOCX、XLSX、PDF、PPTX。
- PDF/PPTX 必须走逐页图片和 OCR；不会只抽文字。
- 旧版 `.doc/.xls/.ppt`、零散图片、音视频、无页码自动配图不在当前范围。
- 飞书和 Obsidian 都保留；Fake Adapter 仅用于测试，不是客户正式目标。
