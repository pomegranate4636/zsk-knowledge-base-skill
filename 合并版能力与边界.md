# 合并版 ZSK 能力与边界

## 合并原则

这次合并以新 ZSK 为骨架，没有把原 Agent 整体塞进 Router。通用建库、Binding、Stage 5—8、Feishu/Obsidian/Fake Adapter 和安装器继续属于 ZSK；原 Agent 的豪宅规则、两次确认、PDF/PPT 图片证据和飞书发布能力分别迁入策略层、确认层、媒体层和发布层。

## 能力落点

| 原 Agent 能力 | 合并后位置 | 结果 |
|---|---|---|
| PDF/PPT 逐页图片与 OCR | `shared/media_renderer.py`、`stage5_intake.py` | 页面 PNG、OCR 哈希、页码清单成为来源主证据 |
| 03 豪宅六类、逐字事实、高风险阻断 | `shared/haozhai_policy.py`、`stage6_knowledge.py` | 可选 `haozhai-v1` 策略，不污染通用核心 |
| 04 固定七段拆解 | `shared/haozhai_policy.py`、`stage7_method.py` | 七段顺序固定、逐字来源、页面回链 |
| 05 三层画像与本人事实复核 | `shared/haozhai_policy.py`、`stage8_profile.py` | 三层隔离、主体一致、确认摘录哈希门槛 |
| 分类确认、完整草稿确认 | `shared/approval.py`、`content_workflow.py` | 两次内容确认跨轮持久，内容变化自动失效 |
| 飞书授权链接和二维码 | `shared/feishu_auth.py` | Agent 主动发起；挑战码不落盘 |
| 来源/正式页发布与图片插入 | `shared/feishu_publish.py`、`feishu_stage5.py` | 两个文档、引用图片、防重标记、插入后回读 |
| 失败续跑和双链接收据 | `shared/feishu_publish.py` | `pending/blocked/published` 可审计，不把半成功说成完成 |
| 首次建库和已有库绑定 | `stage11_bootstrap.py`、`runtime_state.py` | 建库确认跨轮；九根和规则回读后才保存主绑定 |
| 唯一公开入口 | `shared/zsk_entry.py`、`zsk-router/SKILL.md` | 宿主只维护一个运行时门面 |

## 保留下来的 ZSK 通用能力

- 飞书、Obsidian、Fake 三种 Adapter。
- 首次新建知识库和绑定已有知识库。
- 01 来源、02 异常、03 业务知识、04 内容方法、05 Profile 的后端中立合同。
- 不覆盖同名 Skill、不覆盖客户已修改规则的安装与运行边界。
- `haozhai-v1` 是可选策略；非豪宅客户仍可使用通用 ZSK。

## 没有迁入的旧负担

- 原 Agent 的整套本地 Vault 目录不作为第二套正式知识库。
- 不要求客户运行 `pip`、PowerShell 发布脚本或手工寻找飞书权限入口。
- 不保留与当前 ZSK 合同重复的双 Router、双确认文件或另一套发布主入口。
- 不把原始 PDF/PPT 二进制直接发布到飞书；发布转换后的来源正文、页面图片和正式知识页。

## 成功定义

代码测试通过只说明本地合同成立。真实资料只有同时满足以下条件才算 `published`：批准与当前草稿/媒体哈希一致、来源文档可回读、正式知识页可回读、引用图片标记可回读、稳定对象引用一致、收据包含两个飞书链接。任何一步失败都只能是 `pending` 或 `blocked`。
