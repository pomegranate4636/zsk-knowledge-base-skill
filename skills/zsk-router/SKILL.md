---
name: zsk-router
description: 通用知识库项目的唯一公开入口。识别建库、资料入库、状态、说明更新或把已确认的 Obsidian 知识库交接给 Content 口播 Slim，按共享合同绑定一个客户与一个主知识库，并在关键依赖、权限、来源或回读失败时停止。
---

# ZSK Router

## 目的

只让用户面对一个入口，负责把自然语言任务交给正确的内部职责。Router 只编排，不把客户差异写回共享 Skill，也不直接拼接任一后端的凭据或物理路径。

## 当前阶段边界

客户已明确调取 `zsk-router` 后，只要自然语言表达“创建 / 新建 / 搭建知识库”，就进入首次建库向导；不要求固定说法。客户说“把资料入库”时，进入资料路由。资料先由 `zsk-ruku` 统一转为可读 Markdown：MD、TXT、严格 CSV 为轻量本地处理，JSON、HTML/HTM、DOCX、PPTX、XLSX、PDF 使用本机 Microsoft MarkItDown；只产生 01 登记或 02 安全异常。旧版二进制 Office、图片、音视频等其他格式准确停止。

PDF/PPTX 默认仍走轻量文字入库。只有资料的页面版式、图表、截图或完整视觉本身影响含义，或用户明确要求“保留完整每页”，Router 才提出启用通用页级证据模式；必须先说明会额外保存完整页图并取得原件保存授权。启用后，PPT 原生文字按页提取，包含图片的页使用本地 OCR；低置信页面必须提供校对正文才能继续。该模式不包含行业特例、图片理解或猜测式图文对应。

来源已登记、处理权和隐私均通过后，读取 06 中的客户偏好。默认由 AI 按内容单元做语义识别，不要求客户选择“放 03、04 还是 05”：可核验的业务事实交给 `zsk-zhishi`，可迁移的做事或表达方法交给 `zsk-duibiao`，与绑定主体有关的确认事实、运营设定或候选素材交给 `zsk-profile`。同一份资料可以拆成多个内容单元并分别进入 03、04、05；每个单元都保留同一个 `source_id` 和片段说明。

分类本身不确定时保留在 01 并标记“待路由”，不写 02，也不反复让客户选目录。只有隐私、权限、归属、版本、主体冲突、文件损坏、转换失败、写入失败或回读失败才进入 02。客户可在 06 把默认“自动分类”改为“写入前预览”；这是项目级偏好，不写回共享 Skill。

## 首次建库向导

首次使用不要求客户先准备飞书链接、客户名称或 Obsidian Vault。只询问缺失项：后端（飞书或 Obsidian）和知识库名称；Obsidian 再确认保存位置。客户已给出的信息不得重复追问。

- 飞书：先检查 lark-cli、用户身份和创建知识空间权限。未连接时显示“连接我的飞书”步骤，连接完成后再检查；未通过前零写入。通过后才创建私有空间、01—07、AGENTS、README，并回读。只有空间链接和九个根对象都真实返回，才能说“知识库已创建”。
- Obsidian：默认建议 macOS 使用 `~/Documents/知识库名称`，Windows 使用 `C:\\Users\\当前用户\\Documents\\知识库名称`；不默认写入桌面、保险库或隐藏目录。必须展示完整目标路径，让客户确认、改位置或取消后才创建。
- 创建成功后，保留新知识库自己的 06 使用说明，不写回共享 Skill；返回飞书链接或 Obsidian 路径，并提示“知识库已创建，可以开始上传资料”。后续运行由宿主保存并传入该位置；未实现持久绑定时不得声称已自动记住。
- Obsidian 建库成功后，可以提示一次“如果以后要用 Content 口播 Slim，可将这个库设为默认内容资料库”；用户明确同意连接时才读取 [Content 口播 Slim 交接规则](references/content-koubo-slim-handoff.md)，先做零写入预检，展示完整路径和讲述者模式后停下。
- 当前运行环境缺少建库或连接能力时，准确说明缺少什么并停止；不得伪称已经创建，也不得把客户推回“你自己先随便建一个库”的技术流程。

## 必须遵守

- 一次执行只能解析一个稳定绑定；绑定冲突、权限不足、依赖缺失和读回失败都 fail closed。
- 公开调用只进入 `zsk-router`；来源登记、业务知识、内容方法和 Profile 由内部职责承接。
- `source_id` 由原件 SHA256 确定，只留在元数据中用于防重和回链；客户看到的来源目录、原件、可读版和页图使用“日期＋中文标题”的人类可读名称。未完成权限和隐私 Gate 前不得保存原件。
- `03`、`04`、`05`、`indexed_only` 与 `02` 是可解释路由结果，不使用数字评分替代准入 Gate。
- 创建前确认必须绑定预览、客户绑定、后端、模板版本和有效期；错误、过期或已使用的确认零写入。
- Content 口播 Slim 交接确认必须匹配当前预览摘要；只允许 Obsidian，本地 Registry、Manifest 或客户绑定冲突时停止，不得扫描电脑、覆盖旧配置或宣称飞书已直接可用。
- 同名异构、重复根节点、空规则或不可读规则必须停止；客户改过的 AGENTS、README、06 不覆盖，仍可继续入库。
- Obsidian 相对路径、`..`、软链接、特殊对象和未知根对象必须停止，绝不回退个人 Vault。
- MarkItDown 产生但未落地的本地图片占位必须移除，不能让 Obsidian 或飞书显示破图。完整页证据模式下，Obsidian 可读版嵌入真实相对页图；飞书来源 Docx 同时写校对正文与高清原页图，并回读正文、图片数量、尺寸和远端媒体 SHA256。
- 旧版二进制 Office（`.doc/.xls/.ppt`）、零散图片、音视频和正式资产生产不在当前范围。PDF/PPTX 可选择完整页证据，但默认按 06 偏好询问；本地页图 OCR 属于该模式，图片向量检索、图片描述、猜测式图文对应和无确认自动发布仍不在范围。

## 输出

返回当前状态、路由结果、稳定对象引用、原因码和下一步；不把测试通过说成真实后端已连接，不把阶段 1 说成项目首发完成。
